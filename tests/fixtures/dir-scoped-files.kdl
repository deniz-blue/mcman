// One file name in two dirs, which are two different files
target "smp" path="./run/smp" type="server"

dir "plugins/LuckPerms" {
    fs:copy "config/luckperms.yml" "config.yml"
}

dir "plugins/spark" {
    fs:copy "config/spark.yml" "config.yml"
}
